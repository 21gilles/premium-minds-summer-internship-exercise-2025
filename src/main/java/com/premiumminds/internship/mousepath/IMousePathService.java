package com.premiumminds.internship.mousepath;

interface IMousePathService {
  /**
   * Method to validate the grid
   * 
   * @param char[][] grid
   * @return boolean which is the result of the validation 
   */
  public boolean isValidGrid(char[][] grid);
}
